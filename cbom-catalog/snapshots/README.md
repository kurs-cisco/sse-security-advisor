# Snapshots

This directory is the default local output location for
`../scripts/export_db_snapshot.sh`. Real PostgreSQL dumps, checksum sidecars, and
manifests may contain sensitive inventory and ownership data and must not be
committed. Share approved bundles through private encrypted storage.

See [Database snapshot sharing](../docs/DATABASE_SNAPSHOTS.md).

